import logoHorizontalBlack from "@/assets/logo-horizontal-black.png.asset.json";

/** Institutional logo used by structured data (Organization / publisher). */
export const BRAND_LOGO_PATH: string = logoHorizontalBlack.url;

/** Default social preview image (1200x630). */
export const OG_DEFAULT_PATH = "/og-default.jpg";
